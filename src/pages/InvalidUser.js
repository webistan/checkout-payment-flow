import React from "react";

const InvalidUser = () => (
  <div style={{ textAlign: "center", marginTop: "100px" }}>
    <h1>Invalid or Expired Link</h1>
    <p>The link you used is invalid or the user does not exist.</p>
  </div>
);

export default InvalidUser;
